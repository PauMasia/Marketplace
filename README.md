

- Arreglar y mirar conexion base de datos
- Probar que el tema de Models funciona correctamente y no tengo que crear un id y create_uid y create_date en cada uno 
    y lo tienen por default
- Posibilidad de añadir un write_uid, pero claro esto se hace en Odoo con el .write de Models y este tema es muy tedioso
- Añadir logica de calculo para cada vez que se crea y actualiza